Rate limiter blocks access calls and prevent dos attacks. Control the traffic and use the sustem efficiently.

i) start by creating a singlke class wiht 1 fucntion whihch will see if the user already exist and 
if the user has crossed its request for whole time let say 10 time lifetigm then dont allowed next request.
ii)inprovise... next step make it time stamp wise like.. check for each user how many time it can enter in within 5 or 10 seconds
iii) since now we are keeping the rules and all in the same class, we need to sepearte the responsiblities
e.g create a rule calss which will keep ruels for the operation to be performed.
iv) do depency injection so that the classess cannot be tightly coupled ( i.e we are free to pass anything
let say )   .... SOLID CONCEPT GHUSHA DIYA HAI BEECH ME...
class A:
    def __init__(self, b):
        self.b = b we dont have to worry about what b we ar apssing here, rather abc( x, y) hre we are 
        tightly coupled to send this only
v) now seperation of data layer krna tha to hamne pura hata diya bas user_request_store.py me bam bas 
add, read, ermove ka logic rakhe hue hai we baki sab manager class me hoga.
vi) ushke baad apne ko implement karna pdega diffrent algorithms, for rate limiting strategy, and 
that we need to select based on the input.
vii) this implementation will happend by strategy design pattern

I am taking the algo from the clinet so that the manager shall not have to be the knowleedge fo which i am using also 
I am doing dependency injection in the class so not to make it more tightly coupled.
also i am creatign one interface for all the algo, internlly its handlign the the also..
followign OCP, SRP, DIP and ISP
    ###################################  ALGORITHMS  ###################################
i) Token bucket algo:
    - refill the bucket after x time. with y tokens.
    - allows suddend spiks in the traffic. Internet, Amazon, Strip use its
    - Necessasarly use diffrent buckets for diffrent api's let say 1 need to access 100 req/min 
        other as 1M req/sec but other as 1 req/sec we can take 3 diffrent buckets. also some are public api's
        that case use a centralized bucket
    
                        && challanges in production &&
    i) race condition between refill and consume: solved using locks, atomic ops, redis scripts   ( Thread safety ) jaroori hai..
    ii) memory growth lM user === every one has ther own bucket... fucked already.. solverd using
       **  idle bucket eviction ** TTL cleanup **
    iii) Observablity   ( Print a matric where i can see how many times and how does this algo is been used)
    iv) partial server nahi karte hai token bucket me , aggr 5 manga hai 2 hai to reject karenge consume method.
        basically algo ka math strong hona chahiyea, float point handle karo and all.
    v) yah pe issue hai scale me 
    ** let say 1 user 10000 request : Hot key prblme hai ye jo ki : solved using sharding, local cache, distributed limiter
    the main issue hai ye : few users → massive traffic 
    and 1000 user 1000 request 
    to 